import OfficeDashboard from './OfficeDashboard';

// Director dashboard is similar to Office dashboard but with higher-level overview
export default function DirectorDashboard() {
  return <OfficeDashboard />;
}
